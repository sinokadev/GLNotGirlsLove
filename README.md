# GL Not Girls Love!

The collection of OpenGL demos.

## Quick Start

This project uses [just](https://just.systems/) as a command runner.

```bash
$ just setup
$ just # Running main.cpp!
```

or use CMake

```bash
$ cmake -S . -B build
$ cmake --build build
$ ./build/GLNotGirlsLove
```

